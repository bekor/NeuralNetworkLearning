#pragma once

/***
 * This will be responsible to log anything it get into a file or database.
 * At first it should log anything it gets into a file. 
 * It should be formatted.
 ***/

class Logger
{
public:
	Logger();
	~Logger();
};

